import { ClassEntry } from '@/lib/types'
export function TodayTab({ classes }: { classes: ClassEntry[] }) {
  return <div className="text-slate-500 text-sm py-8 text-center">Today tab coming soon</div>
}
