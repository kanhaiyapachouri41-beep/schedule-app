import { ClassEntry } from '@/lib/types'
export function WeekTab({ classes }: { classes: ClassEntry[] }) {
  return <div className="text-slate-500 text-sm py-8 text-center">Week tab coming soon</div>
}
